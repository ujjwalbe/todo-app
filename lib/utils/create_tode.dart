import 'package:flutter/cupertino.dart';

import 'package:flutter/material.dart';

modalBottomSheet(context) {
  return showBottomSheet(context: context, builder: (context) => Container(height: 100, color: Colors.amber,));
}
